* { margin: 0; padding: 0; box-sizing: border-box; }

body {
    font-family: "Segoe UI", Arial, sans-serif;
    background: #2c3e50;   /* fundo escuro */
    color: #ecf0f1;        /* texto claro */
}

/* Cabeçalho */
header {
    background: #000;      /* preto sólido */
    color: #fff;
    padding: 15px 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 1000;
}

header .logo { font-weight: bold; }
header .title { font-size: 16px; }
header .account { cursor: pointer; }

/* Menu lateral */
nav {
    width: 220px;
    background: #1c2833;   /* cinza escuro */
    min-height: 100vh;
    position: fixed;
    top: 60px; left: 0;
    padding-top: 20px;
}

nav ul { list-style: none; }
nav ul li { margin: 15px 0; }
nav ul li a {
    color: #ecf0f1;
    text-decoration: none;
    padding: 10px 20px;
    display: block;
}
nav ul li a:hover { background: #000; }

/* Conteúdo principal */
main {
    margin-left: 240px;
    padding: 80px 20px 20px;
}

/* Cards */
.card {
    background: #34495e;   /* caixa escura */
    padding: 20px;
    margin-bottom: 20px;
    border-radius: 8px;
    box-shadow: 0 2px 5px rgba(255,255,255,0.1);
}

/* Botões */
button {
    background: #3498db;
    color: #fff;
    border: none;
    padding: 10px 15px;
    margin: 5px;
    border-radius: 5px;
    cursor: pointer;
}
button:hover { background: #2980b9; }

/* Inputs */
input, select {
    padding: 8px;
    margin: 5px 0;
    width: 100%;
    border: 1px solid #555;
    border-radius: 5px;
    background: #2c3e50;
    color: #ecf0f1;
}
